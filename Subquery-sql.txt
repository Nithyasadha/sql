CASE 1:

1)WAQTD EMPNO AND ENAME ALONG WITH ANNUAL SALARY OF ALL THE EMPLOYEES IF THEIR ANNUAL SALARY IS GREATER THAN WARDS ANNUAL SALARY.
   select empno,ename,sal*12 from emp where sal*12>(select sal*12 from emp where ename='WARD');  

2) WAQTO NAME AND HIREDATE OF THE EMPLOYEES IF THEY ARE HIRED BEFORE SCOTT
select ename,hiredate from emp where hiredate<(select hiredate from emp where ename='SCOTT');

3) WAQTD NAME AND HIREDATE OF THE EMPLOYEES IF THEY ARE HIRED AFTER THE PRESIDENT
select ename,hiredate from emp where hiredate>(select hiredate from emp where job='PRESIDENT');

4) WAQTO NAME AND SAL OF THE EMPLOYEE IF THEY ARE EARNING SAL LESS THAN THE EMPLOYEE WHOS EMPNO IS 7839.
select ename,sal from emp where sal<(select sal from emp where empno=7839);

5) WAQTD ALL THE DETAILS OF THE EMPLOYEES IF THE EMPLOYEES ARE HIRED BEFORE MILLER
select *from emp where hiredate<(select hiredate from emp where ename='MILLER');

6) WAQTD ENAME AND EMPNO OF THE EMPLOYEES IF EMPLOYEES ARE EARNING MORE THAN ALLEN
select ename,empno from emp where sal>(select sal from emp where ename='ALLEN');

7) WAQTO ENAME AND SALARY OF ALL THE EMPLOYEES WHO ARE EARNING MORE THAN MILLER BUT LESS THAN ALLEN
select ename,sal from emp where sal>(select sal from emp where ename='MILLER') AND sal<(select sal from emp where ename='ALLEN');

8) WAQTD ALL THE DETAILS OF THE EMPLOYEES WORKING IN DEPT 20 AND WORKING IN THE SAME DESIGNATION AS SMITH
select *from emp where deptno=20 AND JOB=(select job from emp where ename='SMITH');

9) WAQTD ALL THE DETAILS OF THE EMPLOYEES WORKING AS MANAGER IN THE SAME DEPT AS TURNER
select *from emp where job='MANAGER' AND deptno= (select deptno from emp where ename='TURNER');

10) WAQTD NAME AND HIREDATE OF THE EMPLOYEES HIRED AFTER 1980 AND BEFORE KING
select ename,hiredate where hiredate>'31-DEC-1980' AND hiredate<(select hiredate from emp where ename='KING');

11) WAQTD NAME AND SAL ALONG WITH ANNUAL SAL FOR ALL EMPLOYEES WHOS SAL IS LESS THAN KING AND MORE THAN 3500
select ename,sal,sal*12 from emp where sal<(select sal from emp where ename='KING') and sal>3500);

12) WAQTD ALL THE DETAILS OF EMPLOYEES WHO EARN MORE THAN SCOTT BUT LESS THAN KING
select *from emp where sal>(select sal from emp where ename='SCOTT') AND sal<(select sal from emp where ename='KING');

13) WAQTD NAME OF THE EMPLOYEES WHOS NAME STARTS WITH 'A' AND WORKS IN THE SAME DEPT AS BLAKE.
select ename from emp where ename like 'A%' AND deptno = (select deptno from emp where ename='BLAKE');

14) WAQTD NAME AND COMM IF EMPLOYEES EARN COMISSION AND WORK IN THE SAME DESIGNATION AS SMITH
select ename,comm from emp where comm is not null and job=(select job from emp where ename='SMITH');

15) WAQTD DETAILS OF ALL THE EMPLOYEES WORKING AS CLERK IN THE SAME DEPT AS TURNER 1
select *from emp where job='CLERK' and deptno=(select deptno from emp where ename ='TURNER');

16) WAQTD ENAME, SAL AND DESIGNATION OF THE EMPLOYEES WHOS ANNUAL SALARY IS MORE THAN SMITH AND LESS THAN KING
select ename,sal,job from emp where sal*12>(select sal*12 from emp where ename='SMITH') and sal*12<(select sal*12 from emp where ename='KING')



CASE 2:

1. WAQTD NAMES OF THE EMPLOYEES EARNING MORE THAN SCOTT IN ACCOUNTING DEPT 
select ename from emp where sal> (select sal from emp where ename='SCOTT') and DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='ACCOUNTING');

2. MAQTD DETAILS OF THE EMPLOYEES WORKING AS MANAGER IN THE LOCATION CHICAGO
SELECT *FROM EMP WHERE JOB='MANAGER' AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOC='CHICAGO');

3. WAQTD NAME AND SAL OF THE EMPLOYEES EARNING MORE THAN KING IN THE DEPT ACCOUNTING
SELECT ENAME,SAL FROM EMP WHERE SAL>(SELECT SAL FROM EMP WHERE ENAME='KING') AND DEPTNO IN (SELECT DEPTNO FROM EMP WHERE DNAME='ACCOUNTING');

4. WAQTD DETAILS OF THE EMPLOYEES WORKING AS SALESMAN IN THE DEPARTEMENT SALES
SELECT *FROM EMP WHERE JOB='SALESMAN' AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='SALES');

5. WAQTD NAME, SAL, JOB, HIREDATE OF THE EMPLOYEES WORKING IN OPERATIONS DEPARTMENT AND HIRED BEFORE KING
SELECT ENAME,SAL,JOB,HIREDATE FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME ='OPERATIONS') AND HIREDATE<(SELECT HIREDATE FROM EMP WHERE ENAME='KING');

6.DISPLAY ALL THE EMPLOYEES WHOSE DEPARTMET NAMES ENDING 'S'.
SELECT *FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME LIKE '%S');

7.MAQTD DNAME OF THE EMPLOYEES WHOS NAMES HAS CHARACTER 'A' IN IT
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE ENAME LIKE '%A%');

8. WAQTD DNAME AND LOC OF THE EMPLOYEES WHOS SALARY IS RUPEES 800
SELECT DNAME,LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE SAL=800);

9. MAQTD DNAME OF THE EMPLOYEES WHO EARN COMISSION
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE COMM IS NOT NULL);

10. WAQTD LOC OF THE EMPLOYEES IF THEY EARN COMISSION IN DEPT 40
SELECT LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE COMM IS NOT NULL AND DEPTNO=40);


#Interview Questions
1)DISPLAY HIREDATE AND JOB OF ALL THE EMPLOYEES WORKING FOR SALES DEPARTMENT
select hiredate,job from emp where deptno in (select deptno from dept where dname='SALES');
select hiredate,job from emp,dept where EMP.DEPTNO=DEPT.DEPTNO AND DNAME='SALES';

2)DISPLAY LOCATION AND DNAME OF EMPLOYEE WHO WORKING AS PRESIDENT WITH 2000 SALARY
SELECT LOC,DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE JOB='PRESIDENT' AND SAL=2000);
SELECT LOC,DNAME FROM EMP,DEPT WHERE EMP.DEPTNO=DEPT.DEPTNO AND JOB='PRESIDENT' AND SAL=2000;

3)DISPLAY THE DNAME OF EMPLOYEES WHOES SALARY IS MAXIMUM SALARY BUT LESSER THAN 3000
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE SAL IN (SELECT MAX(SAL) FROM EMP WHERE SAL<3000));
SELECT DNAME FROM EMP,DEPT WHERE EMP.DEPTNO=DEPT.DEPTNO AND SAL IN (SELECT MAX(SAL) FROM EMP WHERE SAL<3000);

4) DISPLAY THE DEPARTMENT NAME OF THE EMPLOYEES WHO ARE REPORTING TO ADAMS.
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE MGR IN (SELECT EMPNO FROM EMP WHERE ENAME='ADAMS'));  

5)DISPLAY LAST EMPLOYEE RECORD ACCORDING TO EMPNO.
SELECT * FROM EMP WHERE EMPNO IN (SELECT MAX(EMPNO) FROM EMP);

6)DISPLAY ALL THE EMPLOYEE WHOSE SALARY IS GREATER THAN AVERAGE SALARY OF DEPARTMENT 30.
SELECT *FROM EMP WHERE SAL > (SELECT AVG(SAL) FROM EMP WHERE DEPTNO=30);

7)DISPLAY THE NUMBER OF EMPLOYEES WHO WORK FOR RESEARCH DEPT AND THEIR SALARY IS LESSER THAN ONE OF THE SALARY IN DEPARTMENT IN 30.
SELECT COUNT(*) FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH' ) AND SAL < ANY (SELECT SAL FROM EMP WHERE DEPTNO=30);
SELECT COUNT(*) FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH' ) AND SAL < ANY (SELECT MIN(SAL) FROM EMP WHERE DEPTNO=30);

8)DISPLAY THE DNAME THAT ARE HAVING CLERK IN IT.
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE JOB='CLERK');

9)DISPLAY THE DEPARTMENT NAMES THAT ARE HAVING ATLEAST TWO EMPLOYEES WORKING IN
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP GROUP BY DEPTNO HAVING COUNT(*)>1);

10)DISPLAY ALL THE EMPLOYEES WHO ARE JOINED AFTER BLAKE.
SELECT * FROM EMP WHERE HIREDATE>(SELECT HIREDATE FROM EMP WHERE ENAME ='BLAKE');

11)LIST THE DEPT NAME THAT ARE HAVING AT LEAST 3 EMPLOYEES BUT NOT MORE THAN 5 EMPLOYEES IN IT.
SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP GROUP BY DEPTNO HAVING COUNT(*)>2 AND COUNT(*)<5);

12)DISPLAY THE LOCATION OF ALL EMPLOYEES WHOSE REPORTING MANAGER SALARY IS GREATER THAN 2000.
SELECT LOC FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMP WHERE MGR IN (SELECT EMPNO FROM EMP WHERE SAL>2000));

13)SELECT THE ENAME AND SALARY OF THE EMPLOYEES WHOS DNAME IS HAVING AT CHARACTER 'O' IN IT
SELECT ENAME,SAL FROM EMP WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME LIKE '%O%');

14)DISPLAY ENAME,SAL OF EMPLOYEES WHO ARE EARNING MORE THAN ANY OF THE ANALYST.
SELECT ENAME,SAL FROM EMP WHERE SAL>ANY(SELECT SAL FROM EMP WHERE JOB = 'ANALYST');

 
 














